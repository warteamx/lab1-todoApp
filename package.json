{
  "name": "lab1todoApp",
  "version": "1.0.4",
  "description": "Server and Client Development Scripts",
  "main": "README.md",
  "lint-staged": {
    "client/**/*.{js,jsx,ts,tsx,json}": [
      "prettier --write",
      "eslint --fix"
    ],
    "server/**/*.{js,ts,json}": [
      "prettier --write",
      "eslint --fix"
    ],
    "*.{md,yml,yaml}": [
      "prettier --write"
    ]
  },
  "scripts": {
    "server:dev": "npm --prefix ./server run dev",
    "client:dev": "npm --prefix ./client run start",
    "test:client": "npm --prefix ./client run test",
    "test:server": "npm -prefix ./server run test",
    "test:pre-commit": "npm run test:client && npm run test:server",
    "lint:client": "npm --prefix ./client run lint",
    "lint:server": "npm -prefix ./server run lint",
    "lint:staged": "lint-staged",
    "format:staged": "lint-staged",
    "release": "semantic-release",
    "release:dry": "semantic-release --dry-run",
    "version:update": "node scripts/update-version.js",
    "build:version": "npm run build:version:client && npm run build:version:server",
    "build:version:client": "npm --prefix ./client run build:version",
    "build:version:server": "npm --prefix ./server run build:version",
    "changelog:generate": "node scripts/generate-changelog.js",
    "deploy": "node scripts/deploy.js",
    "deploy:staging": "node scripts/deploy.js staging",
    "deploy:production": "node scripts/deploy.js production",
    "version:manual": "node scripts/manual-version.js"
  },
  "devDependencies": {
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/exec": "^7.1.0",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/github": "^11.0.5",
    "conventional-changelog-conventionalcommits": "^9.1.0",
    "conventional-changelog-gitmoji": "^0.2.5",
    "eslint": "^9.32.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.1.6",
    "prettier": "^3.6.2",
    "semantic-release": "^24.2.7",
    "semantic-release-gitmoji": "^1.6.8"
  }
}
